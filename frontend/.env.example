VITE_API_BASE_URL=http://localhost:8080/api/v1

# Used only to redirect the browser to Keycloak's own hosted
# "forgot password" page — the frontend never calls Keycloak's API directly.
VITE_KEYCLOAK_URL=http://localhost:8180
VITE_KEYCLOAK_REALM=ecommerce-oms
VITE_KEYCLOAK_CLIENT_ID=ecommerce-oms-backend